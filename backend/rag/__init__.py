# RAG pipeline modules
# pipeline.py   — ingestion (PDF → chunks → embeddings → ChromaDB)
# embeddings.py — HuggingFace embedding model wrapper
# retriever.py  — ChromaDB similarity search + confidence scoring